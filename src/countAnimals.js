const data = require('../data/zoo_data');

function countAnimals(animal) {
  // seu código aqui
}

// const residentes = data.species[0].

// { species: { residents: residentes } } = data;

// console.log(residentes.length);

module.exports = countAnimals;
